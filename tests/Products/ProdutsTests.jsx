/*Products test will be tricky since it has file upload, so it isn't complete for now*/

// describe('Inserting a new Product, without any image', () =>{
//   it('Should have at least name and price filled and insert after click', () =>{
//
//     var ProductAdmin = TestUtils.renderIntoDocument(<ProductsAdmin />);
//
//
//   })
// })
